<Window x:Class="PerceptualComputingDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Perceptual Computing Demo" Height="350" Width="525" 
        xmlns:sharpGL="clr-namespace:SharpGL.WPF;assembly=SharpGL.WPF">
    <Grid>

        <!-- The OpenGL control provides a OpenGL drawing surface. -->
        <sharpGL:OpenGLControl  
            Name="openGLControl" 
            OpenGLInitialized="openGLControl_OpenGLInitialized"
            OpenGLDraw="openGLControl_OpenGLDraw"
            Resized="openGLControl_Resized"
            DrawFPS="True" 
            RenderContextType="FBO" 
            FrameRate="50" />

        <StackPanel VerticalAlignment="Top"  Orientation="Horizontal" Height="50">
            <Button Style="{StaticResource ButtonStyle}" Click="On_Faster">Faster</Button>
            <Button Style="{StaticResource ButtonStyle}" Click="On_Slower">Slower</Button>
            <Button Style="{StaticResource ButtonStyle}" Click="On_Listen">Listen</Button>
            <TextBox HorizontalAlignment="Stretch" Width="320" Name="tb" Margin="5"></TextBox>
        </StackPanel>
    </Grid>
</Window>
